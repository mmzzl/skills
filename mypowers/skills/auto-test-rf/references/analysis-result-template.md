# 分析报告模板（存根）

> **统一格式定义已迁移至 `auto-test/references/unified-config.md` §5（summary.md 格式）。**
> **分类由 `auto-fix` 统一完成**，分类报告格式详见 `auto-fix/agents/failure-classifier.md`。

## 单个失败用例分析报告

格式详见 `auto-fix/agents/failure-classifier.md` 中的模板。

## 汇总报告

格式见 `auto-test/references/unified-config.md` §5。
