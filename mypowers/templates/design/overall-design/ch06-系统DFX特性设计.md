# 6. **系统DFX特性设计**

*从系统整体层面描述DFX特性的架构方案，重点关注跨模块的架构决策。*

<span style="color:orange">**[层次定位] 本章只写跨模块的架构级DFX方案，不写单个模块内部的实现细节。三层文档的DFX分工：
- 《系统需求文档》§4：定义DFX**指标和要求**（如可用性99.9%、RTO<5min）
- 《总体设计文档》§6（本章）：定义达成指标的**架构方案**（如安全域划分、HA拓扑、监控体系选型）
- 《模块级设计文档》§7：定义各模块内部的**具体实现**（如健康检查接口、重试逻辑、日志格式）

如果你写的内容只涉及单个模块的实现细节，应移到对应的模块级设计文档中。**</span>

---

## 6.1. **安全性设计**

*安全设计的递进逻辑：先确定合规底线（兜底机制），再识别风险（威胁建模），基于风险做架构设计，最后核查供应链合规。*

### 6.1.1. **安全兜底机制合入**

> 必填项。按产品交付形态选择安全兜底机制合入，确保产品默认安全能力防护最佳，暴露面最小化。

*安全兜底机制不满足时需备案：由安全经理完成不满足项分析确认，向安全研发部吴义和11582抄送研发主管发出备案申请，邮件答复结果上传至该处。*

| 安全设计检查项 | 术语解释 | 适用场景 | 安全要求 | 合入指导文档 | 技术支持 | 结果 | 备注 |
|---|---|---|---|---|---|---|---|
| Ngswaf合入 | 软件应用防火墙组件 | 仅支持http协议；支持盒子/XAAS交付类产品 | 覆盖所有外部可直接访问的http协议端口；覆盖所有外部可直接访问的登陆前接口/参数/header | | | | |
| 可信Token合入 | 内核态安全兜底组件 | 仅支持TCP协议；内核可控产品 | 覆盖所有外部可直接访问的TCP协议端口 | | | | |
| HIDS合入 | 主机入侵检测组件 | 仅支持盒子类交付产品；仅适用x86、arm架构的Linux系统 | 合入最新版本hids-agent；默认上报的溯源日志要配置全面；hids-agent配置进程可自重启 | | | | |
| 端口收敛 | 端口配置安全活动 | 适用全部场景 | 仅开启必要的服务和对应的端口；非默认开启的业务端口需配置可访问范围或ip白名单；远程维护类端口默认实现定时关闭功能 | | | | |
| 代码加密 | 源码加密与混淆安全活动 | 适用全部场景 | 涉及到php/python/java/lua/perl自研后端业务代码，要完成代码加密 | | | | |
| 容器安全 | docker基础镜像漏洞、配置 | 适用于容器化部署场景 | 镜像或容器扫描结果中无严重/高危漏洞 | | | | |
| 安全编译 | — | 适用于有C/C++二进制类交付类产品 | 登陆前C/C++二进制端口应用，完成安全编译要求 | | | | |
| 移动应用加壳 | — | 适用于移动终端交付类产品 | 涉及到移动终端（Android/IOS）交付，要使用顶象完成应用加壳 | | | | |
| 数据安全与合规 | — | 适用全部场景 | 不能有类后门功能；敏感数据保护：非必要敏感数据不采集、必要采集的敏感数据要加密/脱敏。海外版本要求：品牌隔离、数据不出合规区 | | | | |
| 网关溯源指纹 | 通过公网IP定位客户信息和版本补丁信息 | 主要适用盒子/分布式场景 | 对外可直接的业务端口都可以获取到加密后的网关溯源文件或字符串内容；溯源字符串中必须包含设备ID、设备版本信息、设备补丁信息 | | | | |

### 6.1.2. **威胁建模分析**

*采用 STRIDE 方法开展威胁建模与安全设计活动。*

| 元素 | 威胁分类 | 威胁场景名称 | 威胁评级 | 威胁分析 | 消减方案 | 残余风险 | 落地版本 | 当前状态 | 测试方法 | 漏洞编号 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| *(外部实体、处理过程、数据存储、数据流)的名称* | *欺骗S/篡改T/否认R/信息泄露I/拒绝服务D/提权E* | *(威胁列表/知识库中选择)* | *严重/高/中/低/风险项* | *详细分析威胁的利用难度及影响* | *针对威胁名称的解决方案* | *残余风险等级* | *消减方案落地版本* | *已解决/处理中/暂不处理/不存在* | *验证威胁存在的攻击/测试流程* | *漏洞编号* | |

### 6.1.3. **整体安全架构**

*基于威胁建模识别出的风险，描述系统的安全架构设计。*

#### 6.1.3.1. **安全域划分**

*说明系统的安全域划分*

| 安全域 | 包含的模块 | 安全等级 | 访问控制 |
|---|---|---|---|
| *公网区* | *Web前端* | *低* | *需认证* |
| *应用区* | *业务服务* | *中* | *内网隔离* |
| *数据区* | *数据库* | *高* | *严格授权* |

#### 6.1.3.2. **数据传输安全**

*说明模块间的数据传输安全机制*

1. *HTTPS加密*
2. *内部服务间认证*
3. *敏感数据加密传输*

#### 6.1.3.3. **其他安全架构设计**

*补充安全域划分和数据传输安全未覆盖的其他安全架构设计，如：认证与授权体系、密钥管理、安全审计架构等。不涉及写"不涉及"。*

### 6.1.4. **子模块安全设计**

*根据威胁建模（§6.1.2）中识别出的风险，逐模块描述安全设计机制。各模块的设计须满足整体安全架构（§6.1.3）的要求。*

#### 6.1.4.1. **模块1安全设计**

*详细描述模块1的安全设计机制。*

#### 6.1.4.2. **模块2安全设计**

*详细描述模块2的安全设计机制。*

*（按实际模块数量继续添加）*

### 6.1.5. **预使用组件版本安全情况**

*使用的开源组件对应版本，不允许有严重高危/可利用漏洞或开源协议风险分数大于等于 65 分的组件。*

---

## 6.2. **可靠性设计**

### 6.2.1. **能力目标定义**

*若立项时已有完善的可靠性需求，可直接继承，但对于不可直接测试的目标（如SLA=5个9）需进一步分解为故障检测率、自动恢复率、恢复时长等可验证指标。若立项时无完善需求，则需进行版本级目标定义（可多级表达，类似OKR分解）。*

*从三个维度定义可靠性目标：*
1. **故障管理维度**：适用于全新产品或重构版本，包含故障检测能力（检测率、检测时间）、故障自动恢复能力（恢复率、恢复时长）、事故人工恢复时长、过载保护能力、升级业务中断。
2. **故障对象维度**：适用于可靠性专项提升版本，按故障对象定义目标，如硬盘故障管理、资源管控等。
3. **业务功能维度**：适用于普通业务增量开发版本，定义业务对故障的容错能力，如故障场景下的业务中断时长或性能下降等。

*一个版本可能涉及多种维度，可叠加定义，去除重合部分。*

*此表格为参考格式，可自行定义结构，但须保证各层级目标之间映射关系清晰，最终层级目标必须可验证、可衡量。*

| 一级目标 | 二级目标 | 三级目标 | 备注 |
|---|---|---|---|
| *目标描述+目标值：例如故障自动检测率达到95%，故障自动恢复率，故障人工恢复时长小于1小时，升级业务中断时间小于10秒等等* | *目标描述+目标值：对上一级目标的分解* | *目标描述+目标值：对上一级目标的分解* | |
| | | | |
| | | | |

### 6.2.2. **故障场景分析**

*分析当前可靠性能力现状，识别与目标之间的差距，这些差距是本版本需要落实的设计需求。*

### 6.2.3. **实现方案设计**

*针对设计需求给出技术方案，包含高可用架构、故障处理、冗余设计、故障管理、升级不中断和应急恢复等方面。*

#### 6.2.3.1. **高可用架构**

*描述系统的高可用架构设计*

1. *负载均衡*
2. *服务冗余*
3. *故障切换*

#### 6.2.3.2. **故障处理**

*说明系统层面的故障检测和恢复机制*

| 故障类型 | 检测机制 | 恢复策略 | RTO目标 |
|---|---|---|---|
| *模块故障* | *健康检查* | *自动重启* | *< 5分钟* |
| *数据库故障* | *主备监控* | *主备切换* | *< 10分钟* |

#### 6.2.3.3. **冗余设计方案**

*给出冗余设计架构图（硬件部件、软件子模块、网络连接等）、实现方式（主备、主从、负荷分担等）、实现原理（切换/仲裁、数据同步、心跳等）、能力指标（切换时间、集群规模等）。*

#### 6.2.3.4. **故障管理方案设计**

*给出故障检测、定位、上报、隔离、恢复机制。按故障类别分别描述，如进程类故障、网络类故障、数据类故障、硬盘类故障等。*

#### 6.2.3.5. **升级不中断业务设计**

*给出热升级、滚动升级等实现原理和流程图。*

#### 6.2.3.6. **应急恢复方案设计**

*给出业务中断或数据丢失场景的关键恢复方案。*

### 6.2.4. **数据可靠性**

*说明数据备份和恢复机制*

### 6.2.5. **韧性设计**

#### 6.2.5.1. **韧性目标定义**

| 过载场景 | 指标项 | 指标值 |
|---|---|---|
| *并发流量超标* | *2倍流量业务成功率* | *98%* |
| | | |

#### 6.2.5.2. **过载场景分析**

*分析可能发生过载的场景、原因、影响及保障需求。*

#### 6.2.5.3. **过载保护设计**

*描述如何处理过载业务以满足韧性目标，包含业务优先级设计、过载控制机制等。*

---

## 6.3. **性能设计**

### 6.3.1. **性能优化策略**

1. *缓存策略*
2. *异步处理*
3. *数据库优化*

### 6.3.2. **性能瓶颈分析**

*分析可能的性能瓶颈及应对方案*

---

## 6.4. **可扩展性设计**

*描述系统保留的可扩展能力及扩展方式。*

### 6.4.1. **水平扩展**

*说明各模块如何进行水平扩展*

### 6.4.2. **垂直扩展**

*说明系统的垂直扩展能力*

---

## 6.5. **可运维性设计**

*可运维性范围包含安装部署、日常运维、巡检、日志管理、故障定位与修复、监控告警、更换扩容、升级补丁等。*

### 6.5.1. **可运维性目标定义**

*按子领域定义目标，如安装包大小、安装时长、安装步骤、升级时长、灰度升级支持、故障定位时间等。*

### 6.5.2. **监控体系**

*描述系统的监控架构*

| 监控层次 | 监控内容 | 监控工具 |
|---|---|---|
| *基础设施层* | *CPU、内存、磁盘、网络* | *Prometheus* |
| *应用层* | *接口响应时间、错误率* | *APM工具* |
| *业务层* | *业务指标* | *自定义监控* |

### 6.5.3. **日志体系**

*描述系统的日志架构*

1. *日志采集*
2. *日志存储*
3. *日志查询*

### 6.5.4. **可监控性设计** ⭐必填

*定义系统层面的业务监控体系，确保运营期间能快速感知业务异常。*

<span style="color:orange">**[必填要求] 不得只写"接入平台监控"。必须明确业务指标定义和采集方案，不可接受只有基础设施监控（CPU/内存）而无业务层监控的设计。**</span>

**业务监控指标设计：**

| 指标名称 | 指标类型 | 采集时机 | 维度标签 | 告警阈值 |
|---|---|---|---|---|
| *例：quota_apply_total* | Counter | 每次配额申请 | tenant_id, status | - |
| *例：quota_apply_latency* | Histogram | 每次申请处理完成 | tenant_id | P99 > 500ms |
| *例：quota_pending_count* | Gauge | 定时采集(1min) | approver_id | > 50 告警 |

**监控覆盖原则：**
1. **业务成功率**：核心功能接口的成功率指标
2. **处理延迟**：关键链路 P50/P95/P99 延迟，通过链路追踪 span 采集
3. **积压深度**：异步队列/待处理任务的积压量，超阈值触发告警
4. **资源使用率**：业务维度的资源消耗（非基础设施），如配额使用率

**告警策略：**
*描述告警分级（P1/P2/P3）、通知方式（企微/邮件/电话）、值班规则*

### 6.5.5. **可运维性设计方案**

*给出达成目标的实现方案，包含关键流程和关键技术。*

---

## 6.6. **兼容性设计**

*从系统整体层面描述跨版本、跨模块的兼容性架构方案*

### 6.6.1. **API版本策略**

| 策略项 | 方案 | 说明 |
|---|---|---|
| 版本管理方式 | *URL路径版本（/api/v1/、/api/v2/）或 Header版本* | *选型理由* |
| 多版本并存策略 | *最多同时维护N个大版本，旧版本废弃周期* | *如：最多2个大版本并存，旧版本废弃前至少保留2个迭代* |
| 废弃通知机制 | *API响应Header中标记Deprecated、文档公告* | *通知下游消费方的方式* |

### 6.6.2. **升级与数据迁移策略**

| 策略项 | 方案 | 说明 |
|---|---|---|
| 升级方式 | *滚动升级 / 蓝绿部署 / 灰度发布* | *选型理由及适用场景* |
| 数据库Schema变更 | *在线DDL / 双写过渡 / 离线迁移* | *如何保证升级期间服务不中断* |
| 数据迁移工具 | *自研脚本 / Flyway / Liquibase* | *迁移脚本管理和回滚方式* |
| 回滚方案 | *升级失败时的回滚步骤和数据回退策略* | *明确回滚的前提条件和时间窗口* |

### 6.6.3. **跨模块兼容性约束**

*说明模块之间的版本兼容约束。如果各模块独立发版，必须说明版本兼容矩阵或向后兼容承诺。*

| 约束类型 | 约束内容 | 说明 |
|---|---|---|
| 接口向后兼容 | *新版本接口必须兼容旧版本调用方* | *只增不删不改已有字段语义* |
| 消息格式兼容 | *新增字段使用Optional，消费方忽略未知字段* | *保证新旧版本模块可混合部署* |
| 模块版本依赖 | *如有强版本依赖，列出版本兼容矩阵* | *明确哪些模块必须同步升级* |

---

## 6.7. **可测试性设计**

*描述测试场景分析与实现方案，如提供的测试接口（查询信息类、故障模拟类等）、测试工具手段等。*

### 6.7.1. **测试环境架构**

| 环境类型 | 用途 | 与生产环境的差异 | 数据策略 |
|---|---|---|---|
| *开发环境* | *开发自测* | *单实例部署，外部依赖可Mock* | *使用构造数据* |
| *集成测试环境* | *跨模块联调* | *全模块部署，缩小规格* | *使用脱敏生产数据子集* |
| *预发布环境* | *上线前验证* | *与生产环境配置一致* | *使用脱敏生产数据* |

### 6.7.2. **跨模块联调策略**

| 策略项 | 方案 | 说明 |
|---|---|---|
| Mock/Stub方案 | *统一Mock服务 / 各模块自行提供Stub* | *当依赖的模块尚未就绪时如何联调* |
| 契约测试 | *是否采用契约测试（如Pact）验证接口兼容性* | *防止模块独立发版导致接口不兼容* |
| 测试数据管理 | *统一测试数据工厂 / 各模块独立构造* | *跨模块场景的数据准备和清理方式* |
| 环境隔离 | *多套并行环境 / 命名空间隔离 / 流量染色* | *多团队并行开发时互不干扰的策略* |

### 6.7.3. **端到端测试设计**

| 测试类型 | 覆盖范围 | 触发时机 | 工具/框架 |
|---|---|---|---|
| *冒烟测试* | *核心链路（创建→查询→删除）* | *每次部署后自动执行* | *自动化测试框架* |
| *回归测试* | *全量功能场景* | *版本发布前* | *自动化测试框架* |
| *故障演练* | *依赖不可用、网络分区等异常场景* | *定期执行（如每月）* | *Chaos Engineering工具* |

---

## 6.8. **可调试性设计** ⭐必填（复杂业务链路）

*针对跨多服务/多步骤的复杂业务链路，设计支持独立验证、增量验证和快速定位的调试能力。*

<span style="color:orange">**[必填条件] 若系统涉及以下任一场景，本章节必须填写：①跨 3 个以上服务的业务链路；②含异步消息/事件驱动流程；③有多步骤状态机的核心业务。否则写"不涉及"并说明原因。**</span>

**复杂链路识别：**
*列出系统中需要特别关注可调试性的关键业务链路：*

| 链路名称 | 涉及服务数 | 是否含异步 | 调试复杂度 |
|---|---|---|---|
| *例：配额申请→审批→扣减链路* | *4个服务* | *是（RocketMQ）* | *高* |

**独立验证设计：**
*每个模块/步骤必须支持独立验证，无需拉通整条链路：*
1. *例：quota-service 提供 dry-run 接口，可在不扣减的情况下验证扣减逻辑*
2. *例：审批服务提供 mock-approve 模式，绕过真实审批人直接通过/驳回*
3. *例：消息消费者支持 replay 模式，可重放历史消息验证处理逻辑*

**增量验证设计：**
*支持对变更内容进行最小范围验证，不需要全链路回归：*
1. *例：每个服务提供 health-check + smoke-test 接口，CI 流水线自动执行*
2. *例：变更仅涉及单服务时，跨服务联调通过 contract test 替代*

**快速定位能力：**
- **TraceID 透传**：全链路必须透传统一 TraceID，日志/监控/告警均可通过 TraceID 聚合
- **步骤状态可查**：业务链路的每一步状态变更必须可通过接口查询（不依赖日志）
- **错误上下文记录**：异常时记录触发场景的完整上下文（入参、当时状态快照、关联 ID）
- **本地模拟能力**：关键外部依赖（消息队列、第三方服务）必须有本地 Mock 方案，支持开发环境调试

---

## 6.9. **可复用性设计**

### 6.9.1. **技术规范落地计划**

*根据产品线的技术规范基线计划，定义本版本需要遵守、实现、新增、修订的技术规范。*

*请填写《技术规范基线计划》，结果以附件的形式保存在本总设文档。*

### 6.9.2. **本版本可采用的公用代码**

*列出可使用公用代码的功能点或子模块。*

| 公用代码 | 使用模块 | 来源 | 备注 |
|---|---|---|---|
| | | | |

> **"来源"列填写规则 — 复用优先级**：
> - **P0 — 中台**：优先使用中台提供的公用组件/服务
> - **P1 — 跨产线**：次优使用跨产线已有的公用代码
> - **P2 — 开源**：再次考虑成熟的开源组件
> - **P3 — 自研**：最后考虑自行开发
>
> 非 P0 来源须在"备注"列或正文中说明降级理由，且须经中台负责人确认无可用组件后方可降级。

### 6.9.3. **本版本可产出的公用代码**

*列出可产出的公用代码，方便其它产品或版本重用。*

### 6.9.4. **设计文档更新约定**

| 设计文档名/子模块名 | 何种情况下需要更新此文档的基线 | 责任人 |
|---|---|---|
| | | |

---

## 6.10. **系统隐私设计**

*定义隐私目标，给出敏感信息保护设计机制，包含传输加密、信息收集说明等。*

---

## 6.11. **软件质量及效能改进设计**

*描述提升软件质量和开发效能的措施，以及对软件设计的影响。*
