# 5. **DFX 特性设计**

*从模块层面详细说明 DFX 特性的实现。*

> **全局要求**：存在总体设计时，模块概要设计的 DFX 特性设计必须承接总体设计 DFX 特性设计的目标、实现方案或工具等。DFX 各维度须逐项评估，受影响项须说明处理方式，不受影响项须注明"不涉及"及原因。

---

## 5.1. **安全性设计**

### 5.1.1. **安全兜底机制覆盖**

*要求：此项为必填项，根据产品已合入的兜底项确定要满足的安全项要求，确保产品默认安全能力防护最佳，暴露面最小化。*

*安全兜底机制不满足时需备案：由安全经理完成不满足项的分析和确认，并向安全研发部抄送研发主管发出备案申请和确认，邮件答复结果上传至该处。*

| 安全设计检查项 | 术语解释 | 适用场景 | 安全要求 | 合入指导文档 | 结果 | 备注 |
|---|---|---|---|---|---|---|
| Ngswaf | 软件应用防火墙组件 | 1、仅支持 http 协议<br>2、支持盒子/XAAS 交付类产品<br>3、仅支持已合入 Ngswaf 类产品 | 1、覆盖子模块所有外部可直接访问的 http 协议端口；<br>2、覆盖子模块所有外部可直接访问的登陆前接口/参数/header，要符合最佳实践要求 | | | |
| 可信 Token | 内核态安全兜底组件 | 1、仅支持 TCP 协议<br>2、内核可控产品<br>3、仅支持已合入可信 token 类产品 | 1、覆盖子模块所有外部可直接访问的 TCP 协议端口；<br>2、代码实现和配置符合可信 token 最佳实践要求 | | | |
| 端口收敛 | 端口配置安全活动 | 适用全部场景 | 1、仅开启必要的服务和对应的端口，根据业务需要配置 iptables 规则；<br>2、非默认开启的业务端口，客户要配置可访问范围或 ip 白名单来开启；<br>3、远程维护类端口，默认实现定时关闭功能 | | | |
| 代码加密 | 源码加密与混淆安全活动 | 适用全部场景 | 涉及到 php/python/java/lua/perl 自研后端业务代码，要完成代码加密 | | | |
| 容器安全 | docker 基础镜像漏洞、配置 | 适用于容器化部署场景 | 镜像或容器扫描结果中无严重/高危漏洞 | | | |
| 安全编译 | - | 适用于有 C/C++ 二进制类交付类产品 | 登陆前 C/C++ 二进制端口应用，完成安全编译要求 | | | |
| 数据安全与合规 | - | 适用全部场景 | 通用要求：1、不能有类后门功能；2、敏感数据保护：非必要敏感数据不采集、必要采集的敏感数据要加密/脱敏<br>海外版本要求：1、FN 产品品牌隔离；2、数据不出合规区 | | | |
| 网关溯源指纹 | 通过公网 IP 定位客户信息和版本补丁信息 | 主要适用盒子/分布式场景 | 1、对外可直接的业务端口都可以获取到加密后的网关溯源文件或字符串内容；<br>2、溯源字符串中必须要包含设备 ID、设备版本信息、设备补丁信息 | | | |

### 5.1.2. **威胁建模分析**

| 元素 | 威胁分类 | 威胁场景名称 | 威胁评级 | 威胁分析 | 消减方案 | 残余风险 | 落地版本 | 当前状态 | 测试方法 | 漏洞编号 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| *（外部实体、处理过程、数据存储、数据流）的名称* | *欺骗S/篡改T/否认R/信息泄露I/拒绝服务D/提权E* | *（威胁列表/知识库中选择）* | *严重/高/中/低/风险项* | *详细分析威胁的利用难度及影响* | *针对威胁名称的解决方案* | *残余风险等级* | *消减方案落地版本* | *已解决/处理中/暂不处理/不存在* | *验证威胁存在的攻击/测试流程* | *漏洞编号* | |

### 5.1.3. **安全设计**

*根据威胁建模中识别出的风险，设计相关的安全机制。按子模块分小节描述各子模块的安全设计机制。*

#### 5.1.3.1. **子模块1安全设计**

*详细描述子模块1的安全设计机制。*

#### 5.1.3.2. **子模块2安全设计**

*详细描述子模块2的安全设计机制。*

### 5.1.4. **开源组件合规性检查（必做）**

*要求：使用的开源组件对应版本，不允许有严重高危/可利用漏洞或开源协议风险分数大于等于 65 分的组件。*

| 组件名称 | 版本 | 许可证 | 协议风险分 | 已知漏洞 | 检查结果 |
|---|---|---|---|---|---|
| | | | | | |

---

## 5.2. **可靠性设计**

*可靠性设计范围：基于可靠性工程与故障容错思路开展，关注运行过程中可能导致特性不可用的故障；不覆盖代码缺陷、业务逻辑不合理、业务条件不满足导致的失效。*

> **AI 生成路径**：承载载体可靠性 → 载体周边可靠性 → 业务流程可靠性（FMEA）。

### 5.2.1. **承载载体可靠性**

*基于特性的实现，识别承载载体并分析其可靠性风险。参考《可靠性特性设计-承载载体分析》，并基于《可靠性需求基线》、《可靠性设计准则库》给出可靠性方案。*

*如果在系统可靠性设计或总体设计中已经包含，可继承相关内容，并说明继承来源。*

### 5.2.2. **载体周边可靠性**

*基于该特性的实现，识别资源使用和周边对象，并分析资源隔离机制及周边可靠性风险。参考《可靠性特性设计-载体周边分析》给出周边可靠性方案。*

*如果在系统可靠性设计或总体设计中已经包含，可继承相关内容，并说明继承来源。*

### 5.2.3. **进程/服务可靠性**

*对于处理用户业务数据流的守护程序、驱动，必须描述所有可能导致用户业务长时间中断的故障可能，并给出合理的解决方法或规避措施。可能导致故障的原因至少包含：程序崩溃、死锁、死循环。*

**进程监控:**

```python
@app.route('/health')
def health_check():
    """
    健康检查接口
    - 检查数据库连接
    - 检查依赖服务
    - 检查资源使用率
    """
    checks = {
        'database': check_database(),
        'cache': check_cache(),
        'disk_space': check_disk_space()
    }

    if all(checks.values()):
        return {'status': 'healthy', 'checks': checks}, 200
    else:
        return {'status': 'unhealthy', 'checks': checks}, 503
```

**自动恢复:**

1. Kubernetes 自动重启 Pod
2. 进程守护程序（systemd）自动重启
3. 健康检查失败自动从负载均衡器摘除

### 5.2.4. **数据可靠性**

**数据持久化:**

1. 数据库事务保证 ACID 特性
2. 定时备份数据库（每天凌晨 2 点）
3. 关键操作记录操作日志

**数据一致性:**

```python
@transaction.atomic
def create_service_with_task(service_data):
    """
    创建服务和任务，保证原子性
    """
    service = Service.objects.create(**service_data)
    task = Task.objects.create(service_id=service.id, type='deploy')
    return service, task
```

### 5.2.5. **业务流程可靠性（FMEA）**

*基于 FMEA 方法，分析每个功能点处理流程中可能发生的故障，以及如何避免这些故障导致功能不可用。*

| 功能点/业务 | 功能要求 | 失效模式 | 失效影响 | 失效原因 | 严重程度（S） | 发生概率（O） | 检测度（D） | AP（措施优先级） | 改进措施 | 改进效果 | 责任部门/人 | 完成时间 | 完成状态 | 重要程度 | 开发自检 | 用例ID |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| *服务创建失败* | *用户可正常创建服务* | *数据库连接失败* | *用户无法创建服务* | *数据库不可用* | *7* | *3* | *2* | *H* | *重试机制+健康检查* | | | | | | | |
| *数据丢失* | *服务数据持久存储* | *磁盘故障* | *服务数据丢失* | *硬件故障* | *9* | *2* | *1* | *H* | *数据备份+主备架构* | | | | | | | |
| *响应超时* | *API 响应 < 200ms* | *数据库慢查询* | *用户体验差* | *查询未命中索引* | *5* | *6* | *3* | *M* | *查询优化+缓存* | | | | | | | |

---

## 5.3. **可测试性设计**

*可测试性设计目标：降低后续测试难度，提高测试效率和质量，尽早发现问题。参考《可测试性技术规范》。*

**AI 生成要求：**

1. 识别测试难度高的业务流程，如逻辑复杂、容易测试不完全的流程，并给出测试方案。
2. 识别依赖性高的业务流程，如依赖特殊硬件、特殊内核版本、外部环境或其他子模块的流程，并给出解耦方法。
3. 识别测试复杂度高的场景，并给出测试方法改进或子模块设计改进措施。

### 5.3.1. **依赖注入**

```python
class ServiceManager:
    def __init__(self, repository=None, event_publisher=None):
        """
        依赖注入，便于测试时 Mock
        """
        self.repository = repository or ServiceRepository()
        self.event_publisher = event_publisher or EventPublisher()

    def create_service(self, data):
        service = self.repository.create(data)
        self.event_publisher.publish('service.created', service)
        return service
```

### 5.3.2. **测试接口**

*提供专门用于测试的接口*

```python
if settings.DEBUG or settings.TESTING:
    @app.route('/test/reset-database')
    def reset_database():
        """测试环境专用：重置数据库"""
        database.truncate_all_tables()
        return {'status': 'ok'}
```

---

## 5.4. **可调试性设计**

*可调试性设计目标：降低开发、测试和客户现场问题定位难度。*

**AI 生成要求：**

1. 识别调试难度高的业务流程，如依赖高并发、大流量、海量数据、复杂/苛刻触发条件，或结果正确性难分析的流程。
2. 给出具体调试方法与降难度措施，如录制数据重放、调试接口诊断、记录内部状态、定时数据有效性自检、校验值自动校验等。
3. 识别客户场景可能出现的故障，如线上异常、难复现问题、技术支持需定位的问题，并给出供技术支持人员诊断错误的方法和工具。

---

## 5.5. **可运维性设计**

*分析本模块为方便运维而在程序内部设计中作出的安排，包括：安装部署、日常运维、巡检、日志管理、故障定位与修复、监控告警、更换扩容、升级补丁等。*

### 5.5.1. **日志设计**

**日志级别:**

| 日志级别 | 使用场景 | 示例 |
|---|---|---|
| DEBUG | 调试信息 | 变量值、函数调用 |
| INFO | 正常操作 | 服务启动、请求处理 |
| WARNING | 警告信息 | 配置缺失、重试 |
| ERROR | 错误信息 | 异常捕获、操作失败 |
| CRITICAL | 严重错误 | 系统崩溃、数据损坏 |

**日志格式:**

```json
{
  "timestamp": "2026-01-01T10:00:00Z",
  "level": "INFO",
  "logger": "service.manager",
  "message": "Service created successfully",
  "context": {
    "service_id": "service-001",
    "user_id": "user-001",
    "request_id": "req-12345"
  }
}
```

**日志存储:**
- 本地日志：/var/log/service/app.log
- 日志轮转：每天或 100MB
- 日志保留：30 天
- 集中日志：推送到 ELK

### 5.5.2. **监控指标**

*定义需要监控的关键指标*

| 指标类型 | 指标名称 | 说明 | 告警阈值 |
|---|---|---|---|
| 业务指标 | *service_create_total* | *服务创建总数* | */ |
| 业务指标 | *service_create_failed* | *服务创建失败数* | *> 10/分钟* |
| 性能指标 | *api_response_time* | *API 响应时间* | *P95 > 500ms* |
| 性能指标 | *database_query_time* | *数据库查询时间* | *P95 > 100ms* |
| 资源指标 | *cpu_usage* | *CPU 使用率* | *> 80%* |
| 资源指标 | *memory_usage* | *内存使用率* | *> 85%* |

**Prometheus 指标示例:**

```python
from prometheus_client import Counter, Histogram

# 服务创建计数器
service_create_total = Counter(
    'service_create_total',
    'Total number of service created',
    ['status']
)

# API 响应时间直方图
api_response_time = Histogram(
    'api_response_time_seconds',
    'API response time in seconds',
    ['method', 'endpoint']
)
```

### 5.5.3. **部署运维**

**部署清单:**

| 部署组件 | 部署方式 | 配置要求 | 启动命令 |
|---|---|---|---|
| *Web 服务* | *Docker 容器* | *2C4G* | *`docker run service:latest`* |
| *Worker 进程* | *Docker 容器* | *1C2G* | *`python worker.py`* |
| *Cron 任务* | *Kubernetes CronJob* | */ | *见 cronjob.yaml* |

**运维手册:**
*提供常见运维操作手册*

1. **服务启动:** `systemctl start service`
2. **服务停止:** `systemctl stop service`
3. **查看日志:** `tail -f /var/log/service/app.log`
4. **数据库备份:** `./scripts/backup.sh`

---

## 5.6. **可扩展性设计**

*说明模块的扩展设计*

### 5.6.1. **插件机制**

*如果支持插件扩展，说明插件机制*

```python
class PluginManager:
    """插件管理器"""

    def __init__(self):
        self.plugins = []

    def register(self, plugin):
        """注册插件"""
        self.plugins.append(plugin)

    def execute_hooks(self, hook_name, *args, **kwargs):
        """执行插件钩子"""
        for plugin in self.plugins:
            if hasattr(plugin, hook_name):
                getattr(plugin, hook_name)(*args, **kwargs)
```

### 5.6.2. **配置驱动**

*通过配置实现功能扩展*

```yaml
# 功能开关配置
features:
  auto_scaling: true
  event_notification: true
  audit_log: false
```

---

## 5.7. **可复用性设计**

### 5.7.1. **本子模块可采用的公用代码**

*列出哪些功能点或子模块可以使用公用代码。*

| 公用代码 | 功能 | 使用方式 |
|---|---|---|
| | | |

### 5.7.2. **本子模块可产出的公用代码**

*考虑产出公用代码，以方便其它产品或版本来重用。*

| 子模块名称 | 功能 | 使用方式 |
|---|---|---|
| *service-client* | *服务调用客户端* | *`pip install service-client`* |
| *common-utils* | *通用工具函数* | *`from common_utils import xxx`* |

### 5.7.3. **设计模式应用**

*说明应用的设计模式*

| 设计模式 | 应用场景 | 示例代码位置 |
|---|---|---|
| *单例模式* | *数据库连接池* | *src/database/pool.py* |
| *工厂模式* | *任务创建* | *src/tasks/factory.py* |
| *策略模式* | *不同类型服务的处理* | *src/services/strategy.py* |
| *观察者模式* | *事件通知* | *src/events/observer.py* |

---

## 5.8. **系统隐私设计**

*隐私目标：系统不会出现因自身安全而造成敏感信息泄露，在收集用户信息时不会私自收集用户不清楚及不同意收集的数据。*

*隐私设计：业务传输过程使用安全协议传输，对敏感内容进行加密传输；明确告知用户收集信息的目的，提供易于阅读的条款或条件摘要。*

---

## 5.9. **跨平台设计和平台差异处理**

*说明该子模块目前及以后可能运行在哪些平台（x86、ARM、MIPS 等，32 位或 64 位系统），在差异处理上做统一规范约定。设计时考虑：对外通讯接口平台无关、数据结构适应 32 位和 64 位系统变迁、能自适应大小端。*

*目标是用统一的一套代码完成需求，避免维护多套代码。可像 Linux 内核那样，把平台无关的流程提炼成公共文件，平台相关的代码写到不同文件。*
