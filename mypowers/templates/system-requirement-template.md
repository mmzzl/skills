---
name: system-requirement-template
description: 系统需求规格说明书模板，覆盖功能性需求、DFX非功能性需求、关键测试点、平台约束与设计任务书，适配 AI-Native 协作要求
phase: requirements
type: template
stage: TR2
status: stable
version: 1.0.0
updated: 2026-04-21
---

# [产品名称] v[版本号] [模块名称] 系统需求规格说明书

> **[AI读取引导]** 本文档定义系统的功能性需求和非功能性需求（DFX），是后续所有设计文档的输入。AI 读取本文档可获取：需求范围与边界、每个需求的正常/异常场景、验收标准、性能/安全/可靠性/可测试性约束、关键测试点、平台约束。**本文档不包含技术实现方案**（见系统级设计文档）。

---

## 文档元信息

| 属性 | 值 |
|---|---|
| 文档类型 | 系统需求规格说明书 |
| 所属产品/模块 | *[产品名 / 模块名]* |
| 所属迭代 | *[迭代版本，如 v2.3]* |
| 文档版本 | V1.0 |
| 文档状态 | 草稿 / 评审中 / 已批准 / 已废弃 |
| 创建日期 | *[YYYY-MM-DD]* |
| 最后更新 | *[YYYY-MM-DD]* |
| 作者 | *[撰写人]* |
| 评审人 | *[评审人]* |
| 输入文档 | *[《市场需求文档》/ 《产品需求文档》/ 《用户需求设计》]* |
| 输出文档 | *[《系统级设计文档》/ 《[模块名]概要设计说明书》]* |
| 关键词 | *[系统核心概念关键词，逗号分隔]* |

---

## 高密度摘要

> **[抗上下文衰减]** 本节是全文关键信息的前置索引，AI 处理长文档时优先读取本节。

**本版本需求范围：** *[一句话描述本次迭代覆盖的功能域]*

**需求总览：**
- 功能性需求：共 *[N]* 个（P0: *[N]* / P1: *[N]* / P2: *[N]*）
- 本次新增：*[N]* 个 | 修改：*[N]* 个 | 删除：*[N]* 个

**全局强约束（必须遵守）：**
- **必须**：所有章节不得为空；如不涉及须说明原因
- **必须**：所有需求须有唯一编号（REQ-XXX 格式）
- **必须**：所有指标须量化（禁止使用"合理""适当""性能较好"等模糊表述）
- **必须**：验收条件须为可客观判断的描述（可用 ✅/❌ 判定）
- **禁止**：在本文档中描述技术实现方案

<span style="color:red">**[AI校验全局规则] 检视本文档时，以下规则全局适用：**
1. **禁止模糊词**：全文扫描"合理"、"适当"、"较好"、"易用"、"尽量"、"性能好"、"响应较快"等表述，发现即标记为问题；所有性能/可靠性/安全指标必须量化（如 p99 < 500ms、可用性 ≥ 99.9%）。
2. **优先级双标记**：功能需求用 P0/P1/P2；约束条件用"必须/应当/可以/暂不"四级；任一需求或约束缺少标记均视为问题。
3. **验收条件可判定**：每条需求的验收标准必须可用 ✅/❌ 客观判断，"功能正常"、"用户满意"等主观描述不可接受。
4. **异常场景有具体示例**：每个需求的异常处理必须有具体触发条件和期望行为，不得只写"异常时返回错误"。
5. **术语全文一致**：同一概念只有一个名称，与 §1.2 定义保持一致。**</span>

**不在本次范围内：** *[明确排除的功能/场景]*

---

# 1. 文档说明

## 1.1 目的

*简要说明本文档的目的，描述当前迭代要解决的问题（满足用户什么需求或解决现有系统什么问题）。*

<span style="color:orange">**[AI可读性要求] 必须同时写明：① 本次迭代做什么；② 本次迭代不做什么（明确排除项）。没有排除项列表的需求文档视为范围不明确。**</span>

**本次迭代范围：**
*[说明本次要实现的核心功能]*

**明确不包含：**
- *[例：不包括 XX 功能，留待下一迭代]*
- *[例：不涉及 XX 模块的改动]*

## 1.2 定义和缩写

<span style="color:orange">**[AI可读性要求] 本节必须填写完整。所有缩写和领域术语须定义；枚举类型字段须列出所有合法取值；同一概念只能有一个名称。**</span>

| 缩写/术语 | 全称/定义 | 备注 |
|---|---|---|
| *[缩写]* | *[全称 / 定义]* | *[如已在上游文档定义，注明引用来源]* |

## 1.3 参考和引用

1. *《[产品] 市场需求文档》vX.X*
2. *《[产品] 用户需求设计》vX.X*
3. *《[产品] DFX 需求基线》vX.X*
4. *[其他参考文档]*

---

## 1.4 版本需求范围

<span style="color:orange">**[AI可读性要求] 此章节必须填写，不得为空。需求内容可能分多个版本迭代，必须明确本版本的边界，避免设计超出范围或遗漏核心功能。**</span>

### 1.4.1 本版本包含

*明确列出本版本迭代要实现的功能范围：*

| 功能模块 | 实现范围说明 | 完成度目标 |
|---|---|---|
| *模块1* | *本版本实现到什么程度* | *基本可用 / 主流场景 / 完整功能* |
| *模块2* |  |  |

### 1.4.2 本版本明确不包含

*列出本版本明确不做的内容（留给后续版本）：*

- *[不做项1]：原因/规划版本*
- *[不做项2]：原因/规划版本*

### 1.4.3 后续版本规划（可选）

*简述后续迭代的大致方向，帮助设计时预留扩展点：*

- *v+1：...*
- *v+2：...*

---

# 2. 关键应用场景

*描述本迭代针对的主要用户场景，每个场景说明：场景描述、用户角色、使用方式、原型/交互依据（如适用）、期望目标。功能性需求将在 §3 中与此处场景关联。*

## 2.1 场景1：[场景名称]

| 项目 | 说明 |
|---|---|
| 场景描述 | *描述业务背景和上下文* |
| 用户角色 | *使用该场景的用户类型（如：管理员 / 普通用户 / 系统管理员）* |
| 使用方式 | *用户如何触达和使用该功能* |
| 原型/交互依据（如适用） | *[原型图 / 交互图链接；不涉及填 N/A]* |
| 操作流程 | *1. 步骤一；2. 步骤二；...* |
| 期望目标 | *用户期望达成的结果（量化描述）* |
| 业务价值 | *该场景对用户/业务的价值* |

## 2.2 场景2：[场景名称]

*按 §2.1 格式继续描述*

---

# 3. 功能性需求

> **[本章要点]** 先用 §3.1 总览表建立全局索引，再在 §3.2 中逐条详细展开。需求完整性要求：隐性需求（如权限检查、操作审计）、衍生需求（如缓存刷新、状态同步）须一并识别。
> **[补充提醒]** 如同一需求在不同产品形态或不同环境下存在差异，请在对应需求中单独标注差异点及适用范围。

## 3.1 需求点清单

<span style="color:orange">**[AI可读性要求] 每条需求：① 唯一编号（REQ-XXX）；② 优先级从 P0/P1/P2 三选一；③ 验收条件须客观可判定，禁止模糊描述。**</span>

| 编号 | 需求点名称 | 优先级 | 需求简述 | 验收条件（一句话） | 关联场景 |
|---|---|---|---|---|---|
| REQ-001 | *[需求名称]* | P0 | *[要实现什么]* | *[如何判断已实现]* | §2.1 |
| REQ-002 | *[需求名称]* | P1 | | | |

## 3.2 需求详细说明

> **[填写约束]** 每条需求须完整填写以下六个子节。无内容填写"不涉及"并说明原因。

---

### REQ-001：[需求点名称]

#### 3.2.1.1 功能描述

*描述不同用户视角下，该功能的形态具体是什么。*

| 用户角色 | 功能形态描述 |
|---|---|
| *[角色1]* | *[从该角色视角看到/使用的功能]* |
| *[角色2]* | *[从该角色视角看到/使用的功能]* |

**功能要求：**
1. *[功能点1：具体描述]*
2. *[功能点2：具体描述]*

**非功能要求：**
1. 性能：*[如：接口响应 p99 < 200ms]*
2. 安全：*[如：须鉴权；操作须写审计日志]*
3. 可靠性：*[如：幂等性要求]*

**验收标准：**
1. *验收条件1*
2. *验收条件2*

#### 3.2.1.2 正常场景

*描述正常场景的步骤及期望效果。*

| 前置条件 | *[触发该功能前系统/用户须满足的条件]* |
|---|---|
| **步骤** | **期望效果** |
| 1. *[操作]* | *[系统反应]* |
| 2. *[操作]* | *[系统反应]* |
| **后置状态** | *[功能完成后系统状态]* |

#### 3.2.1.3 异常场景

*描述异常场景的触发条件、系统表现及期望的错误处理。*

<span style="color:orange">**[AI可读性要求] 必须列举具体触发条件 + 期望行为，不得只写"返回错误"。**</span>

| 异常类型 | 触发条件 | 期望系统行为 | 用户提示 |
|---|---|---|---|
| *例：参数非法* | *例：必填字段为空* | *例：拒绝请求，返回 400* | *例："XX字段不能为空"* |

> 可能出现的异常类型包括但不限于：参数非法、并发冲突、依赖服务不可用、超时、重复提交、权限不足、数据不存在、容量不足等。

#### 3.2.1.4 依赖与风险（关联分析）

**内部功能依赖：**
*[该需求依赖的其他需求编号及说明，如：依赖 REQ-002 的认证功能]*

**外部跨团队依赖：**
*[依赖的外部系统/团队及接口，如：依赖 SCC 提供的资源管理 API]*

**对其他产品或组件的影响：**
*[本需求实现后，会影响哪些其他产品/组件的行为]*

**风险识别：**

| 风险类型 | 风险描述 | 风险等级 | 应对措施 |
|---|---|---|---|
| *例：技术风险* | *例：第三方 API 接口不稳定* | *例：高/中/低* | *例：增加重试+熔断机制* |

> 可能的风险类型包括但不限于：技术风险、测试风险、进度风险、人员风险、合规风险等。

#### 3.2.1.5 当前实现及问题

<span style="color:orange">**[AI可读性要求] 描述该需求所对应功能的当前实现现状。若是全新功能写"当前无实现"；若是改进现有功能，必须说明：① 当前实现描述；② 已知缺陷或不满足用户需求的具体表现。**</span>

**当前实现描述：**

| 维度 | 现状描述 |
|---|---|
| 实现方式 | *[如：当前通过轮询方式实现，每 30s 查询一次数据库]* |
| 涉及模块/代码位置 | *[如：module-xxx / service/YyyService.go]* |
| 数据存储方式 | *[如：存储在 MySQL 表 t_xxx，无索引]* |

**已知问题：**

| 问题编号 | 问题描述 | 影响范围 | 严重程度 |
|---|---|---|---|
| P-001 | *[如：高并发下轮询导致数据库压力过大]* | *[如：影响所有用户的查询响应]* | 高/中/低 |
| P-002 | *[如：状态更新存在竞态条件，偶发数据不一致]* | *[如：影响 X% 的边界场景]* | 高/中/低 |

#### 3.2.1.6 技术核实结论

<span style="color:orange">**[AI可读性要求] 记录该需求的技术可行性核实结果。若已完成技术预研或原型验证，必须写明结论和风险；若未做预研，写"待核实"并标注风险等级。禁止留空。**</span>


| 核实项 | 结论 | 风险等级 | 备注 |
|---|---|---|---|
| 技术可行性 | *可行 / 有条件可行 / 待核实* | *高/中/低/无* | *说明前提条件或限制* |
| 实现复杂度 | *高/中/低* | - | *影响工作量估算的关键点* |
| 依赖外部能力 | *是/否* | *高/中/低/无* | *若是，说明依赖项和是否已确认* |
| 预研结论 | *已完成/未做/不需要* | - | *若已完成，附预研文档链接* |

---

### REQ-002：[需求点名称]

*按 REQ-001 格式（§3.2.1.1 ~ §3.2.1.6）完整填写*

---

# 4. 非功能性需求（DFX）

> **[本章要点]** 所有 DFX 指标须量化（禁止模糊表述）；每项要求须说明测试场景，以支持 §5 中测试点设计。

## 4.1 性能要求

### 4.1.1 性能指标

| 指标项 | 目标值 | 测试场景 | 备注 |
|---|---|---|---|
| *例：响应时间* | *例：< 200ms (P95)* | *例：X 并发下执行 Y 操作* |  |

> 性能指标可覆盖的维度包括但不限于：并发用户数、响应时间（P95/P99）、吞吐量（TPS/QPS）、CPU 使用率、内存使用率等。

### 4.1.2 资源要求

| 资源类型 | 最小配置 | 推荐配置 | 说明 |
|---|---|---|---|
| *例：CPU* | *例：4核* | *例：8核* |  |

> 资源类型可覆盖的维度包括但不限于：CPU、内存、磁盘空间、网络带宽等。

## 4.2 可靠性要求

| 可靠性指标 | 目标值 | 说明 |
|---|---|---|
| 系统可用性（SLA） | *[如：≥ 99.9%（年停机 ≤ 8.76h）]* | |
| 故障恢复时间（RTO） | *[如：< X 分钟]* | *[从故障发生到业务恢复的时间]* |
| 数据丢失容忍度（RPO） | *[如：0 / < X 分钟]* | *[最多允许丢失多少数据]* |
| MTBF（平均无故障时间） | *[如：> X 小时]* | |
| MTTR（平均修复时间） | *[如：< X 小时]* | |

**可靠性机制要求：**

| 机制类型 | 要求描述 |
|---|---|
| 故障检测 | *[如：进程存活检测周期 ≤ Xs；异常 Xs 内上报告警]* |
| 自动恢复 | *[如：进程崩溃后 Xs 内自动重启；重试次数 ≤ X]* |
| 数据备份 | *[如：每 X 小时全量备份；增量备份间隔 Xmin]* |
| 过载保护 | *[如：超过 X 并发时触发限流，返回 429]* |
| 高可用架构 | *[如：双机主备；切换时间 < Xs]* |

## 4.3 安全性要求

### 4.3.1 身份认证与授权

*描述认证方式、权限模型、会话管理要求。*

| 要求项 | 说明 |
|---|---|
| *例：认证方式* | *例：JWT Token；有效期 Xh；续签策略* |

> 可覆盖的维度包括但不限于：认证方式、权限模型、未授权访问、会话安全等。

### 4.3.2 数据安全

| 安全要求 | 说明 |
|---|---|
| *例：传输加密* | *例：全链路 HTTPS/TLS 1.2+；禁止明文传输密码* |

> 可覆盖的维度包括但不限于：传输加密、存储加密、数据脱敏、数据备份安全等。

### 4.3.3 安全防护

| 防护类型 | 要求 |
|---|---|
| *例：注入攻击* | *例：所有数据库操作须使用参数化查询；禁止拼接 SQL* |

> 可覆盖的维度包括但不限于：注入攻击、XSS、CSRF、接口安全、组件安全等。

### 4.3.4 审计日志

*描述操作审计、访问日志的要求。*

| 要求项 | 说明 |
|---|---|
| *例：记录范围* | *例：所有写操作；所有管理员操作；登录/登出* |

> 可覆盖的维度包括但不限于：记录范围、日志字段、保留周期、防篡改等。

## 4.4 可运维性要求

| 运维维度 | 要求（量化） |
|---|---|
| *例：部署方式* | *例：支持 K8s 容器化部署；镜像大小 ≤ XMB* |

> 可运维性可覆盖的维度包括但不限于：部署方式、安装时长、升级方式、回滚能力、监控告警、日志管理、配置管理、巡检能力等。

### 4.4.1 可监控性要求

<span style="color:orange">**[AI可读性要求] 不得泛泛写"接入平台监控"。必须明确业务维度的监控指标定义，包括指标名称、采集方式、告警阈值和处理建议。**</span>

| 监控类型 | 指标名称（具体） | 采集方式 | 告警阈值 | 处理建议 |
|---|---|---|---|---|
| 业务成功率 | *例：配额申请成功率* | *例：接口响应码统计* | *例：< 99% 触发告警* | *例：检查审批服务是否正常* |
| 处理延迟 | *例：配额扣减 P99 延迟* | *例：链路追踪 span 时间* | *例：> 500ms 告警* | *例：检查数据库慢查询* |
| 队列积压 | *例：待审批任务数量* | *例：DB 查询 pending 状态数* | *例：> 100 条告警* | *例：触发告警通知审批人* |
| 资源使用 | *例：配额使用率* | *例：定时任务统计上报* | *例：> 90% 预警* | *例：通知租户管理员扩容* |

**SCC 平台监控接入约束：**
1. *业务指标必须通过 SCC 标准监控 SDK 上报，不得自建监控体系*
2. *告警规则必须在 SCC 运维平台配置，不得硬编码阈值*
3. *关键业务操作（创建/修改/删除）必须有对应的审计日志指标*

## 4.5 可扩展性要求

| 扩展维度 | 要求描述 |
|---|---|
| *例：水平扩展* | *例：无状态设计，支持加节点线性扩展，无需改动代码* |

> 可覆盖的维度包括但不限于：水平扩展、垂直扩展、功能扩展、数据规模等。

## 4.6 兼容性要求

| 兼容性类型 | 要求 |
|---|---|
| *例：浏览器兼容性* | *例：Chrome ≥ 90；Firefox ≥ 88* |
| 不兼容项（明确列出） | *例：不支持 IE 浏览器；不支持 Windows Server 2012* |

> 可覆盖的维度包括但不限于：浏览器兼容性、操作系统兼容性、数据库兼容性、向后兼容性等。

## 4.7 可测试性要求

| 可测试性要求 | 说明 |
|---|---|
| 接口可测试性 | *[所有对外接口须支持独立调用和自动化测试；提供 Mock/Stub 能力]* |
| 状态可观测性 | *[关键业务状态须可查询，便于验证中间状态和最终状态]* |
| 数据可构造性 | *[测试数据可独立构造和清理，不依赖生产数据]* |
| 故障可注入性 | *[支持故障注入（如网络超时、服务不可用），便于可靠性测试]* |
| 环境可隔离性 | *[测试环境与生产环境可独立部署，互不影响]* |
| 日志可追溯性 | *[关键操作有完整日志，便于测试失败时定位问题；支持 traceId 全链路追踪]* |

### 4.7.1 测试工具需求

<span style="color:orange">**[AI可读性要求] 描述本系统测试所需的工具链和集成要求。明确"用什么工具测"、"工具能否接入系统"，避免测试设计脱离实际工具能力。**</span>

| 测试类型 | 使用工具/框架 | 与系统的集成方式 | 接入可行性约束 |
|---|---|---|---|
| 单元测试 | *例：pytest / go test* | *例：CI pipeline 自动触发* | *例：需要 Mock 依赖服务* |
| 接口测试 | *例：Postman / acmp-api-testing* | *例：通过 NoAuth 服务层直接调用* | *例：需要测试环境独立部署* |
| 性能测试 | *例：JMeter / Locust* | *例：需要独立压测集群* | *例：生产数据量级的测试数据构造* |
| 端到端测试 | *例：Playwright / Selenium* | *例：接入 CI/CD 流水线* | *例：依赖前端部署完成* |
| 自动化回归 | *例：千流 CI* | *例：MR 触发自动执行* | *例：需要配置测试用例维护责任人* |

**工具集成约束说明：**
1. *说明哪些测试场景无法自动化，必须人工介入的原因*
2. *说明测试工具对系统侵入性要求（如：是否需要在代码中植入测试钩子）*
3. *说明工具接入的前置条件（如：依赖特定环境变量、服务账号、网络策略）*

## 4.8 开放性要求

*定义哪些功能需要对外提供开放接口/OpenAPI，便于与其他系统或应用集成（特别考虑定制直签需求）。*

<span style="color:orange">**[AI可读性要求] 必须明确区分"对外开放"和"内部使用"的接口。对外开放的接口须定义认证方式、限流策略、版本兼容性承诺。**</span>

| 开放接口名称 | 功能描述 | 对接方 | 接口形式（REST/SDK/Event） | 认证方式 |
|---|---|---|---|---|
| *[接口名]* | *[能力描述]* | *[如：第三方系统/合作伙伴]* | *[REST API]* | *[OAuth2/API Key]* |

*不对外开放的功能（明确列出）：*
- *[如：内部管理接口不对外暴露]*

## 4.9 日志与可观测性要求

<span style="color:orange">**[AI可读性要求] 此章节不得为空。日志级别、保留时长、关键操作覆盖范围须量化说明。**</span>

| 可观测性要求 | 说明 |
|---|---|
| 操作日志 | *必须记录哪些关键操作（如：创建/修改/删除），包含操作人、时间、操作对象、结果* |
| 错误日志 | *必须记录的异常场景；日志级别（ERROR/WARN）；是否需要触发告警* |
| 日志保留时长 | *例：操作日志保留 90 天，错误日志保留 30 天* |
| 链路追踪 | *是否需要 TraceID 串联跨服务调用；哪些接口必须支持* |
| 监控指标 | *必须暴露的业务指标（如：成功率、延迟 p99、队列积压量）* |

---

# 5. 关键测试点

> **[本章要点]** 本章的测试点须与 §3 需求和 §4 DFX 要求一一对应，确保需求得到充分验证。测试点须在需求评审时确认，作为测试用例设计的依据。
> **[补充提醒]** 如同一需求在不同产品形态或不同环境下存在差异，请在对应测试点中分别标注适用形态、适用环境和验证差异。

## 5.1 功能测试点

| 测试点编号 | 测试点名称 | 测试目标 | 关联需求 | 优先级 |
|---|---|---|---|---|
| TC-001 | *[测试点名称]* | *[验证什么功能]* | REQ-XXX | P0 |
| TC-002 | | | | |

## 5.2 性能测试点

| 测试点编号 | 测试场景 | 测试指标 | 预期结果 | 关联需求 |
|---|---|---|---|---|
| PT-001 | *[如：X 并发用户同时执行 Y 操作]* | *[响应时间 p99]* | *[< Xms]* | §4.1 |

## 5.3 可靠性测试点

| 测试点编号 | 测试场景 | 验证点 | 预期结果 | 关联需求 |
|---|---|---|---|---|
| RT-001 | *[如：服务进程异常退出]* | *[自动恢复能力]* | *[< X 分钟恢复]* | §4.2 |
| RT-002 | *[如：依赖服务不可用]* | *[降级/熔断行为]* | *[返回降级响应，不崩溃]* | §4.2 |

## 5.4 安全测试点

| 测试点编号 | 测试场景 | 验证点 | 预期结果 | 关联需求 |
|---|---|---|---|---|
| ST-001 | *[如：SQL 注入攻击]* | *[防注入能力]* | *[攻击被阻止，返回 400]* | §4.3 |
| ST-002 | *[如：越权访问]* | *[权限控制]* | *[返回 403，无数据泄露]* | §4.3 |

## 5.5 兼容性测试点

| 测试点编号 | 测试场景 | 验证点 | 预期结果 |
|---|---|---|---|
| CT-001 | *[如：Chrome 90 访问核心功能]* | *[页面渲染、交互正常]* | *[功能可用，无 JS 报错]* |

---

# 6. 平台约束需求

*描述平台层面的共性开发要求和约束条件。*

## 6.1 各产品功能约束

> **[填写要求]** 明确本系统依赖或对接的各产品（如 SCC、SCP、SKYOPS 等）的能力边界，包括：提供什么能力、不提供什么能力、对本系统的影响。

| 产品名称 | 提供的能力 | 功能约束/限制 | 对本系统的影响 |
|---|---|---|---|
| *[如：SCP]* | *[如：提供资源编排、虚拟机管理]* | *[如：不支持跨 Region 编排]* | *[如：本系统需自行处理跨 Region 场景]* |
| *[如：SKYOPS]* | *[如：提供云端代理通信]* | *[如：单次传输限制 10MB]* | *[如：大文件需分片传输]* |

## 6.2 开发规范约束

**必须**遵守以下规范：

1. 代码规范：*[如：须通过 Lint 检查；禁止硬编码配置]*
2. 接口规范：*[如：REST API 须符合公司接口规范；须提供 OpenAPI Schema]*
3. 数据库规范：*[如：禁止在循环内查询数据库；须使用参数化查询]*
4. 日志规范：*[如：须使用统一日志框架；日志须包含 traceId；禁止打印明文密码]*

## 6.3 平台共性功能要求

### 6.3.1 操作审计

*描述操作审计的要求（涉及的操作类型、字段、存储要求）。*

### 6.3.2 数据同步

*描述数据同步的要求（触发方式、一致性保证、冲突处理）。*

### 6.3.3 多租户支持

*如涉及多租户，描述租户隔离要求（数据隔离级别：DB/Schema/Row）。不涉及请注明"不涉及"。*

### 6.3.4 国际化支持

*如涉及国际化，描述语言支持要求（语言列表、文案管理方式）。不涉及请注明"不涉及"。*

## 6.4 环境约束

| 环境类型 | 约束条件 | 说明 |
|---|---|---|
| 部署环境 | *[如：Kubernetes 1.20+]* | *[容器编排平台要求]* |
| 网络环境 | *[如：内网隔离；出口 IP 白名单]* | *[网络拓扑要求]* |
| 硬件架构 | *[如：x86_64；ARM 暂不支持]* | |
| 操作系统 | *[如：CentOS 7 / Ubuntu 20.04]* | |

---

# 7. 设计任务书

> **[使用说明]** 本章由总体设计人员填写下发给概要设计人员。责任追溯时，如任务书不完整有遗漏，责任在总体设计人员；概要设计人员须自检是否达到任务书要求，在"自检"列填写对应的设计章节号。

## 7.1 需求跟踪表

| 编号 | 需求点名称 | 需求点说明/验收条件 | 自检（概要设计对应章节） |
|---|---|---|---|
| REQ-001 | *[引用 §3.1]* | *[引用 §3.2.x.6 验收标准]* | *[如：§3.1.2]* |
| REQ-002 | | | |

## 7.2 模块整体目标

### 7.2.1 性能目标

| 指标项 | 目标值 | 自检 |
|---|---|---|
| *例：响应时间* | *例：< 200ms (P95)，参见 §4.1* | *填写对应的设计章节号* |

> 性能指标包括但不限于：并发用户数、响应时间、吞吐量、CPU 使用率、内存使用率等，目标值引用自 §4.1。

### 7.2.2 资源开销目标

| 资源类型 | 目标值 | 自检 |
|---|---|---|
| *例：CPU* | *例：4核，参见 §4.1.2* | *填写对应的设计章节号* |

> 资源类型包括但不限于：CPU、内存、磁盘空间、网络带宽等，目标值引用自 §4.1.2。

### 7.2.3 DFX 目标

| DFX 维度 | 目标 | 自检 |
|---|---|---|
| *例：安全性要求* | *例：参见 §4.3* | *填写对应的设计章节号* |

> DFX 维度包括但不限于：安全性要求、可靠性要求、可运维性要求、可扩展性要求、兼容性要求、可测试性要求等，目标值引用自 §4.2~§4.8。

## 7.3 流程要求

| 项目 | 说明 |
|---|---|
| 是否需要外部评审？ | *[是/否；按设计流程要求]* |
| 审核人 | |
| 评审时间 | |

---

# 8. 需求优先级说明

| 优先级 | 说明 | 时间要求 |
|---|---|---|
| P0 | 核心功能，缺失导致主流程不可用 | 当前迭代**必须**完成 |
| P1 | 重要功能，影响用户体验但有降级方案 | 当前迭代完成，可延后到迭代末期 |
| P2 | 增强功能，锦上添花 | 后续迭代完成 |

---

# 9. 修订记录

| 修订版本号 | 作者 | 日期 | 简要说明 | 影响范围 |
|---|---|---|---|---|
| V1.0 | | | 初始版本 | |
